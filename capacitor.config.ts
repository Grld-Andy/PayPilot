import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paypilot.app',
  appName: 'PayPilot',
  webDir: 'dist/paypilot/browser'
};

export default config;
